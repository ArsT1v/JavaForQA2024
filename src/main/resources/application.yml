spring:
  jpa:
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/
    username: postgres
    password: postgres
    database: postgres_db
    driverClassname: org.postgresql.Driver